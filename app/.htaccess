# Nega acesso HTTP directo a esta pasta (código-fonte, nunca deve ser servido).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
