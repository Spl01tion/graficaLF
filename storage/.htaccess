# Nega acesso HTTP directo a esta pasta (logs, emails simulados em dev).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
